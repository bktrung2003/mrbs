/** Sticky header cell for the schedule time axis (day + week grids). */
export function ScheduleTimeColumnHeader() {
  return (
    <div className="flex flex-col items-center justify-center border-r border-slate-100 px-1 py-3 text-center">
      <div className="text-[10px] font-semibold tracking-wide text-slate-500 uppercase">
        Time
      </div>
    </div>
  )
}
