# Paste into Portainer stack → Environment variables (or upload as .env file).
# Replace values below before deploy.

# Required — must NOT be "changethis" in production
DB_PASSWORD=your-strong-db-password
SECRET_KEY=your-long-random-secret-key
FIRST_SUPERUSER_PASSWORD=your-admin-password

# Public URL users open in browser (no trailing slash)
FRONTEND_URL=https://mrbs2.fusionhotelgroup.com

# API is proxied via frontend nginx at /api — no separate VITE_API_URL needed.

# Optional — email
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# EMAILS_FROM_EMAIL=noreply@example.com
